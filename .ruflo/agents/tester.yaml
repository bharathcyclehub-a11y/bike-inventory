type: tester
version: "3.5.0"
capabilities:
  - verification
  - regression-check
  - edge-cases
role: |
  After implementation, verify the fix works.
  Check every page/component affected by the change.
  Test edge cases: empty data, large data, unexpected input.
  Run the build. It must pass.
