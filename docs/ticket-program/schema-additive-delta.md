# Additive schema delta (safe for overnight)

Migration name (full): `20260731_stock_journaling_cd_persistence_expense_category_table_attendance_recurring_failures`

Apply THESE additive, backward-compatible changes to `prisma/schema.prisma` overnight (new enums, new tables, nullable columns, back-relations, indexes). They are reversible by DROP. Do NOT db push (no DATABASE_URL in cloud) — only edit schema.prisma + `npx prisma generate`.

## Apply (dependency order)
- ENUM CdOutcome { NA ELIGIBLE CAPTURED PARTIAL MISSED } — NEW (INV-011). No deps. Must exist before VendorBill.cdOutcome. Stays an enum by design (type safety; billedTo-style free String rejected).
- ENUM FailureSeverity { LOW MEDIUM HIGH CRITICAL } — NEW (INV-009). No deps. Must exist before RecurringFailure.severity. Stays enum (<20 fixed values).
- ENUM AttendanceImportStatus { PROCESSING PARSED COMMITTED FAILED } — NEW (INV-014), mirrors BrandStockUploadStatus lifecycle. Before AttendanceImport.status.
- ENUM AttendanceStatus { PRESENT LATE HALF_DAY ABSENT WEEKLY_OFF HOLIDAY LEAVE UNKNOWN } — NEW (INV-014). Before AttendancePunch.status. Set may extend once the real Petpooja export is seen.
- ENUM AttendanceSource { PETPOOJA_IMPORT MANUAL } — NEW (INV-014). Before AttendancePunch.source.
- InventoryTransaction.location StockLocation? — ADD nullable (INV-001). Single-location INWARD/OUTWARD/ADJUSTMENT stamp; nullable because historical rows have none and TRANSFER uses from/to. Reuses existing StockLocation enum (schema:143), no enum change.
- InventoryTransaction.fromLocation StockLocation? + toLocation StockLocation? — ADD nullable (INV-001), TRANSFER rows only. Intentionally mirrors TransferOrderItem.fromLocation/toLocation (schema:1135-1136).
- InventoryTransaction @@index([location]) + @@index([type, location, createdAt]) — ADD (INV-001) to serve reports/movement + ai/reorder-suggestions groupBy; complements existing indexes (schema:424-429).
- VendorBill.cdDeadline DateTime? — ADD (INV-011). Snapshot = billDate + Vendor.cdTermsDays, frozen at bill CREATE.
- VendorBill.cdEligibleAmount Float? — ADD (INV-011). Snapshot = round(amount * Vendor.cdPercentage/100) at CREATE.
- VendorBill.cdPercentageSnapshot Float? + cdTermsDaysSnapshot Int? — ADD (INV-011). Freeze the Vendor.cd* terms used so history is not rewritten when Vendor.cd* later changes.
- VendorBill.cdOutcome CdOutcome @default(NA) — ADD (INV-011). Terminal per-bill state. Depends on CdOutcome enum.
- VendorBill.cdAmountSaved Float @default(0) + cdAmountLost Float @default(0) — ADD (INV-011). Rolled-up captured vs forgone; lost lives on the bill because an unpaid bill can lapse past deadline with no payment row to attach to.
- VendorPayment.paidOnTime Boolean? — ADD (INV-011). date-normalized paymentDate <= bill.cdDeadline; null when the settled bill had no CD terms.
- VendorPayment.cdAmountSaved Float @default(0) — ADD (INV-011). CD captured by THIS settling payment. Coexists with existing cdDiscountAmount (schema:609). FLAG Ibrahim: confirm whether cdDiscountAmount should keep reducing payable.
- MODEL RecurringFailure — NEW (INV-009): id String @id @default(cuid()); name String; category String @default("general") (free string like AppProblem.category, NOT enum); severity FailureSeverity @default(MEDIUM); description String? @db.Text; isActive Boolean @default(true) (soft-delete); createdById String + createdBy User @relation("FailureCreatedBy"); occurrences FailureOccurrence[]; createdAt/updatedAt; @@index([isActive]); @@index([severity]). Depends on User + FailureSeverity.
- MODEL FailureOccurrence — NEW (INV-009): id; failureId + failure RecurringFailure @relation(onDelete:Cascade); auditCycle String; occurredAt DateTime @default(now()); note String? @db.Text; status String @default("open"); stockCountId String? + stockCount StockCount? @relation (nullable FK, linked later); loggedById + loggedBy User @relation("OccurrenceLoggedBy"); createdAt; @@unique([failureId, auditCycle]) (repeatCount = occurrence count = distinct cycles); @@index([failureId]/[auditCycle]/[status]). CREATE AFTER RecurringFailure, StockCount, User.
- MODEL AttendanceImport — NEW (INV-014), mirrors BrandStockUpload: id; fileName; fileType; status AttendanceImportStatus @default(PROCESSING); periodStart/periodEnd DateTime? @db.Date; totalRows/matchedRows/unmatchedRows Int @default(0); columnMap Json?; notes String?; uploadedById + uploadedBy User @relation("AttendanceUploadedBy"); punches AttendancePunch[]; createdAt/updatedAt; @@index([status]); @@index([createdAt]). Depends on User + AttendanceImportStatus.
- MODEL AttendancePunch — NEW (INV-014): id; importId String? + import AttendanceImport @relation(onDelete:Cascade) (nullable so MANUAL punches survive without an import); userId String? + user User (null until mapped); employeeExtId String?; employeeName String (always kept); date DateTime @db.Date; checkInAt/checkOutAt DateTime?; workedMinutes Int?; status AttendanceStatus @default(UNKNOWN); source AttendanceSource @default(PETPOOJA_IMPORT); rawShift String?; isException Boolean @default(false); exceptionKind String?; notes String?; createdAt/updatedAt; @@unique([userId, date]) (Postgres treats NULL userId as distinct, so unmatched staging rows allowed); @@index([date]/[importId]/[userId,date]/[isException]). CREATE AFTER AttendanceImport, User, AttendanceStatus, AttendanceSource.
- MODEL EmployeeMapping — NEW (INV-014), mirrors BrandSkuMapping: id; employeeExtId String?; employeeName String; userId + user User; createdAt; @@unique([employeeName]); @@index([userId]). Add @@unique([employeeExtId]) ONLY if Petpooja codes are confirmed stable. Depends on User.
- User — ADD back-relations only, ALL merged into the single relations block (schema:165-210): createdFailures RecurringFailure[] @relation("FailureCreatedBy"); loggedFailureOccurrences FailureOccurrence[] @relation("OccurrenceLoggedBy") (INV-009); attendancePunches AttendancePunch[]; attendanceImports AttendanceImport[] @relation("AttendanceUploadedBy"); employeeMappings EmployeeMapping[] (INV-014); [optional] expensesPaid Expense[] @relation("ExpensePaidBy") (INV-003). No column change here (except optional below).
- User.employeeExtId String? @unique — ADD (OPTIONAL, gated on Ibrahim, INV-014) to store the Petpooja code directly for auto-mapping future imports.
- StockCount — ADD back-relation failureOccurrences FailureOccurrence[] (INV-009), after items[] (schema:453). No column change.

## HELD — do NOT apply overnight (destructive / needs snapshot + Ibrahim)
- DROP ENUM ExpenseCategory (schema:68-77) — REQUIRED (INV-003/005). Postgres shares the type/table namespace, so the enum type must be dropped before a table of the same name can be created; they cannot coexist even transiently.
- MODEL ExpenseCategory — NEW TABLE replacing the enum (INV-003/005): id String @id @default(cuid()); code String @unique (= old enum values e.g. FOOD_TEA + new PORTER — preserves backfill and the bank-statements categoryMap); label String; icon String @default("🧾") (emoji glyph, no lucide/Tailwind dep — for non-literate Iqbal); color String @default("slate") (TOKEN key only, mapped to Tailwind classes in a shared helper; never store raw class strings); sortOrder Int @default(0); isActive Boolean @default(true); createdAt/updatedAt; expenses Expense[].
- Expense.category ExpenseCategory (enum, schema:648) -> categoryId String + category ExpenseCategory @relation(fields:[categoryId], references:[id]) (INV-003/005). Swap @@index([category]) (schema:661) -> @@index([categoryId]). Requires ExpenseCategory table created + 9 seed rows + data backfill BEFORE categoryId is set NOT NULL.
- Expense.paidById String? + paidBy User? @relation("ExpensePaidBy") — ADD (RECOMMENDED, gated on Ibrahim, INV-003/005). KEEP existing paidBy String (schema:650) as a denormalized name snapshot for back-compat/search/reports. If adopted, add User.expensesPaid Expense[] @relation("ExpensePaidBy").

## Collisions / shared-model notes
- NAMING (hard): the new model ExpenseCategory reuses the exact name of the enum ExpenseCategory (schema:68). In Postgres, enum types and tables share one namespace, so the enum type MUST be dropped in the same migration BEFORE the table is created — they cannot coexist even transiently. Verified the enum is referenced only by Expense.category (schema:648); BankTransaction.suggestedCategory (schema:718) is a free String and is unaffected. src also carries a TS mirror union in src/types/index.ts (must be widened to the DB rows) and one categoryMap in src/app/api/bank-statements/[id]/review/route.ts (relies on the code column matching old enum values).
- SHARED MODEL User: edited by 3 features at once (INV-009 createdFailures+loggedFailureOccurrences; INV-014 attendancePunches+attendanceImports+employeeMappings+optional employeeExtId; INV-003 optional expensesPaid). Not semantically conflicting, but ALL back-relations must be merged into the one User block in a single migration or Prisma validation fails when the first referencing model is created. New relation names (FailureCreatedBy, OccurrenceLoggedBy, AttendanceUploadedBy, ExpensePaidBy) are all unique vs existing User relation names — no clash.
- SHARED TABLE AppSetting (key/value): written by 3 features (INV-001 SALE_DEDUCT_LOCATION + SALE_DEDUCT_LOCATION_BCC; INV-014 attendance_config; INV-006 optional command_connectors). NO schema collision — distinct keys, data rows only, zero column change. Do NOT add columns.
- cdAmountSaved added to BOTH VendorBill and VendorPayment (INV-011) — different models, not a clash, but semantically distinct (bill = rolled-up captured total; payment = amount captured by that one settling payment). Keep separate; do not unify.
- INV-001 InventoryTransaction.fromLocation/toLocation intentionally duplicate the existing TransferOrderItem.fromLocation/toLocation pair (StockLocation?, schema:1135-1136) — deliberate parallel, not a conflict.
- NO conflict from INV-002 or INV-006: both require zero schema change (User.permissions/customRoleName/navTabs, AlertConfig.rolePermissions, Role enum already exist; connector registry is static TS + env). Nothing to migrate.

## Reversibility
"Use named Prisma migrations, NOT `prisma db push` — the enum drop and the drift reconcile are destructive and db push gives no down path. Recommend splitting into 3 named migrations for a clean rollback story: (A) ADDITIVE SCHEMA — all 5 new enums, 6 new models, the nullable InventoryTransaction/VendorBill/VendorPayment columns, new indexes, and all User/StockCount back-relations; fully reversible by DROP, safe to auto-generate with `prisma migrate dev`. (B) EXPENSECATEGORY ENUM->TABLE — MUST be hand-edited: the naive `migrate diff` output drops Expense.category and destroys category data. Its down-migration is non-trivial (recreate the enum type, map row ids back to codes, restore the column), so snapshot the Expense table before applying. (C) DATA BACKFILLS — ship as an idempotent seed/SQL script, not a schema migration, gated behind a saved snapshot of Product.currentStock + all StockLevel rows + VendorBill CD fields, because the INV-001 drift reconcile is lossy (historical outwards can't be re-attributed to a location) and therefore cannot be exactly reversed. Verify after each: build exits 0, SUM(StockLevel)==currentStock per product, count(Expense)==count(categoryId NOT NULL), and the CD report renders from persisted fields. Deploy/apply must be done by a Vercel-team git author (repo deploy rule) — a non-member author silently blocks the build."
